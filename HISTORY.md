# HISTORY

## timeline-scope の導入（2026-06-12）
名前付きタイムラインは子孫方向にしか伝播しないため、それまでは api-source が受信側の先祖である必要があった。複数のソースから受け取るにはその数だけ入れ子が深くなり、別のソースから受け取る要素を兄弟に置くこともできない。

`timeline-scope` を使うと、宣言した要素の配下ならどこで定義されたタイムラインでも参照できる。配線が DOM から名前に移り、送る側も受け取る側も全員兄弟のフラットな構造になった。`*-scope` 系は範囲を狭めるものだと思っていたので、拡張に使えると気づいたときは構造をどこまで変えることになるか焦ったが、実際の変更は小さく済んだ。

6/25 の api-source 化（ラッパーが自分でデコードする形から、名前付きタイムラインを放送するだけの発信源へ）は、これが前提になっている。

## クエリパラメータ方式からの転換（2026-06-25）
css-api は当初 `?width=current-time&height=random` のように、width と height にそれぞれ機能を指定する 1 エンドポイントだった。

問題は 2 つ。width と height のどちらに何を載せるかを呼ぶ側が毎回決めなければならないのに、2 つに違いがなく、決める意味がない。しかも `?width=/weather/now&height=/datetime/now` という見た目は、2 つのパラメータに関係があることを前提にしているように読めてしまう。

もう一つは、当時のエンコードが `width = value % BASE`、`height = floor(value / BASE)` で両方を値の運搬に使い切っていたため、ステータスを載せる場所がなかったこと。CSS 側は受け取った値が信用できるかを知る手段を持たない。

機能ごとのパスに分け、height の下位を `900 × 値 + (status − 100)` としてステータス専用に予約した。これで HTTP ステータスが CSS から読めるようになり、`--api-ok()` による成否分岐が成立した。

## mapping の属性統合（2026-07-15）
当初は `[mapping]`（初期化時の順で index を配る）と `[selfmapping]`（自分の位置から index を配る）の排他な 2 属性だった。同じ行為の別手法なので同居させる意味がなく、排他にしていた。

HTML の属性に「これとこれは排他なので注意」という慣習はない。方式の違う 2 種であっても、どちらか一方を選ぶものなら 1 つの属性の値としてまとめる方が慣習に合う。そのため `[mapping]` の値を `true` / `self` / `false` の 3 値に統合した。

## generic な monospace への依存をやめた（2026-07-15）
何も変更していないのに macbook-keyboard のレイアウトが崩れた。原因は Osaka フォントをインストールしたことで、総称ファミリ `monospace` の解決先が変わったこと。Chrome の日本語ロケールでは固定幅フォントの既定値が Osaka を指していて、未インストールの間だけ別のフォントに落ちていた。設定を触っていないので、リセットしても戻らない。

Osaka は等幅だが「全角:半角 = 2:1」ではない。裏切られたのは等幅性ではなく 2:1 という慣習の方で、`ch` を単位に幅を組む設計はその比率に暗黙に依存していた。総称ファミリを指定すれば 2:1 が来るという保証はどこにもない。

macbook-keyboard は px 単位で文字送りを調律している部品なので、フォントを明示して固定した。SVG のテキスト画像は全 OS の閲覧者に配るため単一フォントに固定できず、フォールバック列にした上で、文字幅の見積もりを安全上界に取る形にした（幅が過大なら余るだけで済み、不足したときだけ文字が切れる）。

## デコード方式の変遷（2026-06 〜 07-30）

### 2026-06 逆数デコード
`scroll(self)` でスクロール可能量を読み、`round(1 / x − 1)` で値に戻す方式。往復できる上限は 316781。有効数字 6 桁の丸めを 2 回通るため、これより大きいと等式が成立しなくなる。値を width と height に分けて `316781 × h + w` で合成し、運べる範囲を広げていた。

別の壁として、`<integer>` 文脈に 999999 を超える値を渡すと汚染が起きるため、運用上の上限は 999999。

寸法は負にできないので値も 0 起点で、負数は運べなかった。気温は絶対温度の 10 倍で持ち上げて回避していた。

### 2026-07-28 view() の線形読み
逆数を通さず、進行度から値を直接読む方式に変更。6 桁の丸めの壁が消えた。値にオフセットを足して読み側で引き戻すことで、負数も運べるようになった。

計測用の枠が必要で、枠の寸法も予算を食うため上限は ±4195327。

### 2026-07-30 枠のサイズを 0 にする
`animation-range` の起点を `exit` にすることで、枠のサイズを 0 にしても読めるようにした。予算が丸ごと値域になり、上限は ±8388607。

### 2026-07-30 scroll(self) に戻す
`animation-range` の開始と終了の両方に `-100%` を置けば、`view()` を使わずに同じ線形読みができると判明。`exit` も `reverse` も不要になり、記述が短くなった。上限は変わらず ±8388607。

到達点は width / height の上限 16777214px と、その半分の ±8388607。最初の 316781 から片側で 53 倍。制約は実装の都合ではなく、ブラウザが持てる寸法の上限そのものになった。
